---
apiVersion: v1
kind: Pod
metadata:
  name: nginx
spec:
  containers:
    - name: nginx
      image: nginx:1.7.1
      ports:
        - name: web
          containerPort: 80
          protocol: TCP
